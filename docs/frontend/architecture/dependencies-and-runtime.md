# Frontend Dependencies and Runtime

**Status:** Skeleton

This document will define the approved Next.js runtime, dependency responsibilities, rendering boundaries, provider rules, configuration, and dependency governance.
