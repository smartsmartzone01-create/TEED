# Real storefront mode: both values are required.
STOREFRONT_API_URL=http://127.0.0.1:8000
STOREFRONT_SITE_KEY=

# Demo catalog/site data is available only when explicitly enabled.
# Never enable this on a real customer storefront deployment.
STOREFRONT_DEMO_MODE=false
